"use client";

import { Button } from "../components/ui/button";
import { ChevronLeft, ChevronRight } from "lucide-react";
export function JournalCarouselControlsView({ targetId }: { targetId: string }) { const scroll = (direction: -1 | 1) => { const target = document.getElementById(targetId); if (target) target.scrollBy({ left: direction * Math.max(target.clientWidth * .82, 280), behavior: "smooth" }); }; return <div className="hidden items-center gap-2 sm:flex" aria-label="Прокрутка подборки"><Button variant="plain" type="button" onClick={() => scroll(-1)} className="grid size-9 place-items-center rounded-lg bg-[var(--journal-surface-cta)] text-[var(--text-secondary)] transition hover:bg-[var(--accent-soft)] hover:text-[var(--accent)]" aria-label="Прокрутить влево"><ChevronLeft className="" aria-hidden /></Button><Button variant="plain" type="button" onClick={() => scroll(1)} className="grid size-9 place-items-center rounded-lg bg-[var(--journal-surface-cta)] text-[var(--text-secondary)] transition hover:bg-[var(--accent-soft)] hover:text-[var(--accent)]" aria-label="Прокрутить вправо"><ChevronRight className="" aria-hidden /></Button></div>; }
